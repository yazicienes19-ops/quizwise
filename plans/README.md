# Animation improvement plans

Generated by the `improve-animations` skill from a full-codebase motion audit (2026-08-07). Full findings table (17 items) was presented to the user; these 4 are the pre-launch set the user selected — everything else was deliberately deferred to post-launch per the user's own prioritization.

| # | Title | Severity | Status | Depends on |
| --- | --- | --- | --- | --- |
| [001](001-fix-animate-in-duration-system.md) | Fix the broken `animate-in` entrance-animation system | HIGH | DONE | — |
| [002](002-fix-streak-star-stale-state.md) | Make the streak star update when activity is recorded in the same session | HIGH | DONE | — |
| [003](003-accessibility-hover-and-reduced-motion.md) | Gate `:hover` motion behind `(hover: hover)` and honor `prefers-reduced-motion` systemically | HIGH | DONE | — |
| [004](004-confidence-panel-interruptible-reveal.md) | Make the quiz confidence-selector panel interruptible | MEDIUM | DONE | **001** (reuses the `--ease-out` token it introduces) |

## Recommended execution order

1. **001** first — it's the root-cause fix (broken entrance system) and the only dependency another plan has on it.
2. **002** and **003** can run in parallel with each other and with 001 — they touch entirely disjoint files (`streakService.ts`/`Layout.tsx` for 002; `tailwind.config.cjs`/`app.css`/`GraphCanvas.tsx` for 003) and don't share any code path with 001's `app.css` changes (003 appends to a different section of `app.css` than 001 touches, but re-read the file fresh before editing either — do not apply 001 and 003 to `app.css` from stale, pre-fetched content).
3. **004** last, and only after 001 is actually applied and verified — it hard-depends on `--ease-out` existing in `app.css`.

## Deferred (not planned this round — user's explicit call)

Kurz nach Launch: sidebar `transform` instead of `width` (finding #3), Wissensnetz `x/y/scale` → full `transform` string on the canvas (finding #4, "measure before optimizing" per user — check actual node counts in production graphs first), `transition-all` → scoped properties across ~43 files (finding #9).

Later: findings #11, #13, #14, #15, #16, #17 from the original audit table — splash-screen bounce restyle, toast exit animation, node/edge physicality polish, progress-bar `scaleX`, token consolidation. Notably #13 (replace `SplashScreen.tsx`'s `animate-bounce` with a slow fade/glow/scale-0.98→1.00, since a bounce reads as playful against StudeArc's calm/editorial/Apple-Linear-esque style) — the user explicitly agreed this is the right direction, just not urgent enough for this round.

## Status legend

TODO → IN PROGRESS → DONE. Update this table and each plan's `Status` field as work lands. Run `improve-animations reconcile` to re-check this directory against the current code state (marks done plans DONE, refreshes stale file:line references, retires findings that got fixed incidentally).
